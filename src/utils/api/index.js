import API, {
  FETCH,
  POST,
  PUT,
  DELETE,
} from './client'

import { handleErrorMessage } from './errorDefault';

import { createUrl } from './utils';

export { 
  handleErrorMessage,
  createUrl,
  FETCH,
  POST,
  PUT,
  DELETE,
}

export default API